import React from "react";

class SecondaryTimer extends React.Component {
    constructor(props) {
        super(props);
    }
    render() {
        return (
            <div className="SecondaryTimer">
                SecondaryTimer
            </div>
        );
    }
}

export default SecondaryTimer;